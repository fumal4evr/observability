meta {
  name: Hello - Instance 2
  type: http
  seq: 2
}

get {
  url: {{instance2}}/api/hello
  body: none
  auth: none
}

docs {
  Simple hello endpoint that returns instance information.
  This calls Instance 2 on port 8081.
}

tests {
  test("Status code is 200", function() {
    expect(res.status).to.equal(200);
  });
  
  test("Response has instance name", function() {
    expect(res.body.instance).to.equal('instance-2');
  });
}
